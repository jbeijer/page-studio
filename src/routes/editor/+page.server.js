// Handle editor page on server side
export function load() {
  return {
    // Pass any data needed for client-side rendering
  };
}

// Page not suitable for prerendering
export const prerender = false;